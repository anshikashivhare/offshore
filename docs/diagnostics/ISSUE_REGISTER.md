# Issue Register

| ID | Severity | Component | File | Function/Class | Evidence | Impact | Reproduction | Expected Behaviour | Current Behaviour | Recommended Fix | Blocking? | Confidence |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| DIAG-001 | P0 | API/Routing | `backend/app/api/v1/endpoints/routes.py` | `plan_route()` | `if getattr(settings, "DEMO_MODE", False):` | Bypasses core persistence and injects demo mode straight into `astar_planner` | Trigger API with DEMO_MODE=True | `DEMO_MODE` should be handled by a specific demo orchestrator | Core API leaks demo states directly to A* | Isolate `DEMO_MODE` handling outside core API routes | Yes | High |
| DIAG-002 | P0 | Data Ingestion | `backend/app/ingestion/adapters/sea_ice.py` | `get_data()` | `return {"type": "mock_nsidc", "data": "raw_bytes"}` | Production system cannot use mock data | Run ingestion without active mock switch | System should fail explicitly if source is down | System silently returns mock data | Remove mock fallbacks from production adapters | Yes | High |
| DIAG-003 | P1 | Risk/Reliability | `backend/app/ingestion/utils/reliability.py` | `fallback_handler` | `return value or fallback` | Missing data falls back to empty/zero risk | Disconnect DB/Network, check risk | Explicit `PARTIAL/UNKNOWN` state | Silently defaults to fallback | Implement strict `Unavailable` states | Yes | High |
| DIAG-004 | P0 | Testing | `tests/test_iceberg_pipeline.py` | `test_pipeline()` | `assert "SYNTHETIC_PROTOTYPE" in sources` | Tests validate against synthetic data, not real production data | Run pytest | Tests should fail if real data is structured differently | Tests pass securely but give false confidence | Add tests with actual recorded real-world payloads | No | High |
| DIAG-005 | P2 | Frontend | `frontend/src/pages/Home.tsx` | `Home()` | `setRouteValidationError("No vessels available... demo config")` | Frontend implies reliance on demo fallback | Clear DB and load frontend | Should display clear unavailability | Displays demo fallback errors | Clean up demo/mock hardcodes in UI | No | High |
| DIAG-006 | P2 | Backend | `backend/app/api/v1/endpoints/routes.py` | `plan_route()` | `comparison = await comparison_service... demo_mode=demo_mode` | Overloaded API handler | Examine `routes.py` | API should just parse requests and call an Orchestrator | API handler contains complex business and demo logic | Move logic to `RouteOrchestrator` | No | Medium |
| DIAG-007 | P0 | Data | `backend/app/utils/geojson.py` | `parse_geom()` | `fallback = {"type": "Point", "coordinates": [0.0, 0.0]}` | Returns Null Island (0,0) as a valid geometry on error | Send invalid geometry | Raise `ValueError` | Returns [0.0, 0.0] silently | Remove fallback and raise exception | Yes | High |
