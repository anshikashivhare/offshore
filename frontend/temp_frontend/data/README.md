# Data

Shared datasets and data-processing artifacts for OFFSHORE. Large generated datasets should not be committed directly; use the repository's data-ingestion workflow or external storage when appropriate.
